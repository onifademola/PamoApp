
@(Html.Kendo().ComboBox()

    .Name("MTHeadTeacherRemark")
    .DataTextField("Description")
    .DataValueField("Description")
    .Filter("contains")
    .HighlightFirst(true)
    .Suggest(true)
    .Placeholder("select / personalize your comment...")
    .BindTo((IEnumerable<SIM.Model.ViewModel.ReportRemarkVm>)ViewData["rmks"])//ViewBag.NormeringList)
    .HtmlAttributes(new { data_value_primitive = true })
)

@*@(Html.Kendo().ComboBox()
          .Name("RemarkTextEdit")
          .DataTextField("Value")
          .DataValueField("Text")
          .Filter("startswith")
          .DataSource(source =>
          {
              source.Read(read =>
              {
                  read.Action("RemarksListForGrid", "Remark");
              })
              .ServerFiltering(false);
          })
)*@